name: world_map
description: Flutter plugin for animated map. Created for Innopolis University Flutter course.
version: 1.0.0
homepage: 'https://github.com/Danilum/FlutterProject'

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  assets:
    - assets/
